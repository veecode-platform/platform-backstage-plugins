import { makeStyles } from "@material-ui/core";

export const useTotalCostStyles = makeStyles({
    content:{
        width: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between'
    }
})