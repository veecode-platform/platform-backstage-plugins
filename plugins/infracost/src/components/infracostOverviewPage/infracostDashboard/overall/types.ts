export type OverallProps = {
    totalMonthlyCost: string,
    projectName: string,
    baselineCost: string,
    usageCost: string
}

export type OverallTablePros = {
    id: string,
    baseline_cost: string;
    usage_cost: string;
}