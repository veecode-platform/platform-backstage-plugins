import { makeStyles } from "@material-ui/core";

export const useLoadingProgressStyles = makeStyles({
    loadingContent:{
        width: '100%',
        height: '100%',
        minHeight: '65vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
    }
})